IDEAS / Parking lot for v0.2+

- Batch `http_post` events and add configurable batch size / interval.
- Add lightweight unit tests for `netlab.events` and `netlab.output`.
- Add richer scenario parameter types & validation (currently all params are strings from CLI).
- Consider adding a `netlab status --json` output for programmatic checks.
- Add optional prometheus metrics exporter behind a local, non-networked socket (not in v0.1).
- Add a `netlab run --dry-run` mode to show preflight results.
